name: noirs_flutter_frog_playground
description: Practice playground for Dart Frog — routes, middleware, requests, and Dart backends.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.13.1

dependencies:
  equatable: ^2.1.0
  json_annotation: ^4.12.0

dev_dependencies:
  build_runner: ^2.16.1
  json_serializable: ^6.14.1
  test: ^1.25.0
  very_good_analysis: ^11.0.0
